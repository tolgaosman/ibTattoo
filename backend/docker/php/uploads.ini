; The admin panel sends images as base64 JSON, which inflates the payload by
; ~33%. These ceilings sit above the 8 MB image cap enforced in UploadController
; so a valid image is never cut off by the transport before validation runs.
upload_max_filesize = 20M
post_max_size = 20M
